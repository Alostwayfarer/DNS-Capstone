pipeline {
    agent any
    
    stages{
        stage('fetch code') {
                steps{
                       git url: 'https://github.com/Alostwayfarer/DNS-Capstone.git'
                }
        }
        stage('scan'){
        script{
            withSonarQubeEnv(installationName: 'Sonar-Qb-Server'){
                sh '${scannerHome}/bin/sonar-scanner'
                        }
                    }
                }
            }
}